# Bridging

Optional SwiftUI helpers for conditional layouts and scroll containers. Import the ``HIGDesignBridging`` product when you need these utilities without pulling them into every target.

## Helpers

- ``HIGConditionalView``
- ``HIGScrollableContainer``
- ``higConditional(_:if:else:)``