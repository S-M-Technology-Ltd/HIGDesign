/// Fill style for ``HIGCover`` when using the solid-background convenience.
public enum HIGCoverStyle: Sendable, Equatable {
    /// Accent fill with on-accent labels (marketing / brand banners).
    case accent
    /// Secondary surface fill with primary labels (neutral covers).
    case neutral
}
